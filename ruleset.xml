<?xml version="1.0"?>
<ruleset name="Basic">
    <description>The Basic coding standard.</description>

    <!-- Basic coding standard contains PSR-1 and PSR-2. -->

    <!-- Include the whole PSR-1 standard -->
    <rule ref="PSR1"/>
    <!-- Include the whole PSR-2 standard -->
    <rule ref="PSR2"/>
</ruleset>
